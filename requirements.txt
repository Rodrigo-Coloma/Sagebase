# Streamlit Cloud installs from this file.
# Tuned for the free tier (~1 GB RAM): no torch, no local sentence-transformers.
# Embeddings via OpenAI; vector store via ChromaDB; generation via Anthropic.

streamlit>=1.40

# medlit core
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
tenacity>=8.2
lxml>=5.1
loguru>=0.7
pyyaml>=6.0
python-dateutil>=2.9
tqdm>=4.66
orjson>=3.10
numpy>=1.26
tiktoken>=0.7
langchain-text-splitters>=0.2

# Ingestion (PubMed/Crossref/Unpaywall use httpx; bioRxiv too)
# Skipping `arxiv` and `biopython` — not needed for cloud demo.

# Document processing
pymupdf>=1.24

# Vector store: ChromaDB (in-process, persistent)
chromadb>=0.5
rank-bm25>=0.2.2

# Embeddings (cloud default)
openai>=1.40

# Generation
anthropic>=0.39
